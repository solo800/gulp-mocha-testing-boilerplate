const assert = require('chai').assert;

describe('Your first test!', () => {
    it('should pass because the test is fake', () => {
        // do some stuff here for your test
        assert.strictEqual(true, true);
    });
});